services:
  - type: web
    name: crabble
    runtime: node
    plan: free
    buildCommand: npm install -g pnpm@9 && pnpm install --frozen-lockfile
    startCommand: node server/index.js
    healthCheckPath: /
