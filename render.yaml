services:
  - type: web
    name: crabble
    runtime: node
    plan: free
    buildCommand: corepack enable && pnpm install --frozen-lockfile
    startCommand: node server/index.js
    healthCheckPath: /
